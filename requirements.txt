# Core for your renderer & schema checks
reportlab==4.*
svglib==1.*
pillow==10.*
jsonschema==4.*

# Claude API - compatible with langchain-anthropic
anthropic>=0.52.0

# Handy extras you might use later (safe to keep)
requests==2.*
pydantic==2.*
